{
  "name": "job-hub-pro",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "antd": "^3.26.18",
    "node-sass": "^4.14.1",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-scripts": "2.1.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "docker-build": "docker build -f Dockerfile -t job-hub-pro-frontend:prod .",
    "docker-build-up": "docker-compose -f docker-compose.yml up --build",
    "docker-up": "docker-compose -f docker-compose.yml up",
    "docker-down": "docker-compose -f docker-compose.yml down"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
